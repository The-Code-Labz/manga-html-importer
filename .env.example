# Manga HTML Importer API configuration
PORT=4050

# Optional API key. If set, all /parse and /parse-text requests must include:
#   x-api-key: <your-key>
# Leave empty to allow unauthenticated access (only for private networks).
API_KEY=

# Upload limits
UPLOAD_MAX_FILES=10
UPLOAD_MAX_SIZE_MB=50
